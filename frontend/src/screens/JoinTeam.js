import React from 'react'
import Filter from './../components/Filter';

function JoinTeam({history}) {
    return (
       <Filter history={history} isModal={false}/>
    )
}

export default JoinTeam
